# devimage_ubuntu
ubuntu image for development environment

## Node.js version policy

- `nvm` installer version is pinned with `NVM_VERSION`.
- Node.js is pinned with `NODE_VERSION` (not `--lts` tracking).
